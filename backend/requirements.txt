# Web framework
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
python-multipart>=0.0.9

# HTTP (used by the RAG pipeline to call Ollama REST API)
httpx==0.27.2
requests==2.32.3

# Fuzzy matching (slang_normalizer, POI / gate matching)
rapidfuzz>=3.9.0,<4

# RAG - Vector Store (ChromaDB, persistent local storage)
chromadb==0.5.23

# RAG - Embeddings (runs fully offline after first model download)
sentence-transformers==3.2.1

# RAG - Vector Search (FAISS, optional secondary index)
faiss-cpu==1.9.0.post1
# Graph support
networkx==3.3

# OCR for image processing (boarding pass extraction)
opencv-python==4.10.0.84
pytesseract==0.3.10
Pillow==10.4.0

# Offline TTS (PyPI Piper — `pip install piper-tts`; see app/services/tts_service.py)
piper-tts>=1.4.0,<2
