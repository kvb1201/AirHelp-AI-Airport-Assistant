# Web framework
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2

# HTTP (used by the RAG pipeline to call Ollama REST API)
httpx==0.27.2
requests==2.32.3

# RAG - Vector Store (ChromaDB, persistent local storage)
chromadb==0.5.23

# RAG - Embeddings (runs fully offline after first model download)
sentence-transformers==3.2.1

# RAG - Vector Search (FAISS, optional secondary index)
faiss-cpu==1.9.0.post1
# Graph support
networkx==3.3
